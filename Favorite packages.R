-------------------
#Favorite libraries
-------------------
  
  
#dplyr - glimps() function is replacement for str() 
  # data manipulation
  # select() for subsetting
  # mutate() for calculating new columns
  # filter() for selecting certain rows
  # arrange() for reordering rows
  # summarise() computes aggregate functions
  # pipe comes from this package
  install.packages('dplyr')
  library(dplyr)

#tidyr - Written by Hadley Wickham 
    # Data clensing
  install.packages('tidyr')
  library(tidyr)
  
  
# lubridate - modify date values easily
#     dmy, hms ect
  install.packages('lubridate')
  library(tidyr)
  
# stringr - string manipulation 
  install.packages('stringr')
  library(stringr)
  
  
# purrr - replaces for loops with functional programming
  # map and walk functions replace the need for for loops. sorta replaces *apply family
  
  
# corrplot::corrplot  gives a nice way to generate a correlation matrix
  
  
  #rpart - best known and widely used decision tree package
    # rpart creates the model.  Then use plot to show the tree
  
  #aplpack - makes bagplot() available
  #bagplots - mixture of a boxplot and a scatterplot
    
  
  # broom - tidy() function gathers results from linear models into DF
  
  #gganimate - wrapper for animate package for using ggplot objects
  
  
  ### caret - package for ML tasks.  'train' function in particular is very powerful
  
  
  ### caTools - contains colAUC() for generating ROC curves